{
    "name": "rails_api_with_auth",
    "engines": {
        "node": ">=20.0.0"
    },
    "scripts": {
        "dev": "foreman start -f Procfile.dev",
        "clean": "rm -rf public",
        "build": "cd frontend && pnpm install && pnpm run build && cd ..",
        "deploy": "cp -a frontend/dist/. public/",
        "app-postbuild": "pnpm run clean && pnpm run build && pnpm run deploy"
    }
}